import SongDetails from '../Components/SongDetails';

export default function Show() {
    return (
        <div>
            <h2>The Show Page!</h2>
            <SongDetails />
        </div>
    );
};